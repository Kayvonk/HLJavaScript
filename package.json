{
  "name": "high-level-js-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/prebuild.mjs",
    "dev": "npm run prebuild && npm --prefix client run dev",
    "build": "npm run prebuild && npm --prefix client run build",
    "preview": "npm --prefix client run preview"
  }
}
